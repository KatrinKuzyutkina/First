@startuml Расстановка корабля
start
if (Какой режим?) then (Ручной)
    :Пользователь рисует корабль;
    while (Проверка)
        if (Есть ошибки?) then (да)
            switch (Какая ошибка?)
            case ( корабль большой)
                :"Корабль слишком большой!";
            case (                много кораблей такой формы)
                :"Уже достаточно n-палубных кораблей";
            case ( корабли пересекаются)
                :"Корабли пересекаются";
            endswitch
        else (нет)
            :Отображает корабль на поле;
            if (Все корабли рассталены?) then (да)
                :"Игра началась. Ваш ход!";
                break
            else (нет)
                :Пользователь рисует следующий корабль;
            endif
        endif
    endwhile (всё верно)
else (Авто)
    :Приложение рандомно расставляет корабли, 
    проверяя не пересекаются ли корабли, 
    не выходят ли за границы поля, 
    расположены ли все корабли;
endif
:Отображает все корабли на поле;
stop
@enduml
